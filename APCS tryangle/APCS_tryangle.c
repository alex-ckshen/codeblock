#include<stdio.h>
#define total 3

int input[total];

int main(void){
    scanf("%d %d %d", &input[0], &input[1], &input[2]);

    for(int current_sort = 1;current_sort<total;current_sort++){
        for(i=1; )
    }

    return 0;
}
